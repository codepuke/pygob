---
title: Installation
---

# Installation

```bash
pip install pygob
```

Or with [uv](https://docs.astral.sh/uv/):

```bash
uv add pygob
```

pygob has no dependencies beyond the Python standard library. Nothing needs to
be compiled, and Go does not need to be installed to use it — Go is only
required to regenerate this project's own test fixtures.
